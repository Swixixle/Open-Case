## What is true right now
Branch `feature/rotate-leaked-open-case-api-key-de-hardcode-from`; IGNITION session active until this park. `main` is unchanged at `945f14a` — the rotation touched only gitignored files, so there is nothing to commit. The leaked Open-Case admin API key formerly hardcoded in `scripts/run_cruz_once.sh` is ROTATED and de-hardcoded: a fresh key was minted for handle `localhost_d495f9e7` via `POST /api/v1/auth/keys` (which revokes the prior key by overwriting its stored SHA-256 hash), stored in `.env` as `OPEN_CASE_API_KEY` (gitignored), and the script now reads `AUTH="Authorization: Bearer ${OPEN_CASE_API_KEY:?set OPEN_CASE_API_KEY}"` — zero hardcoded tokens. Verified against the live local server: new key → `POST /cases` 200; old key → 401 (revoked). Both `.env` and the script are gitignored, so `git status` is clean and the key can never be committed. No key value was ever printed. Belt unchanged: 13 tests pass, delta anchors correctly (`constraints_changed: False`).

## What changed in the last session
Rotated the leaked localhost admin token and fixed the root cause (no belt work). Confirmed the mechanism in `routes/auth.py` first (re-issue overwrites the stored hash → old key dies; a revoked key hits `require_api_key`'s `row is None` → 401). Started the local sqlite-backed server (`uvicorn main:app`), minted a new key with `X-Admin-Secret`, appended it to `.env` as `OPEN_CASE_API_KEY`, and de-hardcoded `scripts/run_cruz_once.sh` via `sed` (env var + `:?` fail-fast) so the token no longer lives in the file. Proof: new key `POST /cases` = 200, old key = 401; script env-wiring tested both ways (set → header built, unset → fails fast with message). Recalibrated risk mid-task: this was a localhost admin token (BASE defaults to 127.0.0.1:8000), not a third-party cloud credential — hygiene, not emergency. One throwaway `RotationVerify` case row was created in the local dev DB during verification (harmless). Server stopped afterward. No git commits (both edited files gitignored).

## What needs to happen next
SESSION 2 — build `test-health` (belt tool #3): first feeder adding a new signal beyond delta; gitignored append-only history at `belt/state/test-health-history.jsonl` (`belt/state/` already gitignored), pure analysis functions + hermetic tests, `--run`/no-run/`--json` modes; afterward wire test-health into violations' `FEEDERS` registry (small; ask for spec). Deferred: PART B in Cowork — add the handed-off violations startup step to the ignite `SKILL.md`, verify via a live ignite run. Residual security note: rotation only revoked the key in the LOCAL sqlite DB — if a deployed Open-Case instance ever accepted that key, mint a fresh one there too (almost certainly moot given the localhost-only token). Optional follow-ups: normalize `/park` to full-timestamp filenames (belt-and-suspenders vs the mtime/checkout fragility); investigate/remove the nested `/Open-Case/` repo; set `ANTHROPIC_API_KEY` if touching LLM code.

## What is true right now
Branch `main` at `945f14a`; IGNITION session `20260710T061827Z-fix-delta-anchor...` active until this park. The full belt arc is now on main: `8f95399` (delta), `97425ad` (violations), `df9d407` (gitignore), `a684606` (track `.memory/`), and `945f14a` (delta anchor fix). delta's `resolve_anchor` now ranks session logs and derives `--since` by `effective_dt = max(filename timestamp, file mtime)`, so date-only `/park` logs (which parse to midnight) no longer undersort beneath a same-day full-timestamp log. 13 belt tests pass. On this repo delta now anchors to `session-2026-07-10-merge-track-...` (since `05:54:08Z`), `constraints_changed: False`, and violations shows no warn (only a notice for uncommitted files). Documented limitation: mtime-based anchoring is fragile to a fresh `git clone`/`checkout` (mtimes reset). `/park` still writes readable date-slug filenames — the reader handles them; the writer was deliberately not normalized.

## What changed in the last session
Fixed a delta anchor correctness bug (commit `945f14a`, fast-forward merged to main). Root cause: delta ranked session logs purely by filename-parsed timestamp; `/park`'s date-only names parse to midnight, so a same-day full-timestamp log (`...T01-35-13Z`) outranked later date-only parks — delta anchored to a stale 01:35Z session, swept in 3 later sessions, and fed a false `constraints_changed: True` to violations. Fix: `resolve_anchor` uses `effective_dt = max(filename_dt, mtime)` for ranking and `--since`, which also repairs legacy on-disk logs, not just future writes. Updated the 2 anchor tests that conflated filename-date with `since` (now pin mtimes via `os.utime`); added 2 regressions — the actual failing case (same-day date-only written later than a full-timestamp log) and an mtime tiebreak between two same-day date-only logs. Verified on the real repo via before/after table: anchor moved from stale `01:35Z` to the merge-track park (`05:54:08Z`), `constraints_changed` flipped True→False, warn gone. Design choice: fixed the reader (delta); left `/park` writing readable date-slug names.

## What needs to happen next
TOP PRIORITY (non-belt): rotate the live bearer token embedded in `scripts/run_cruz_once.sh` — gitignored now, but a live credential sat in plaintext in the working tree. Then SESSION 2: build `test-health` (belt tool #3) — the first feeder adding a new signal beyond delta; gitignored append-only history at `belt/state/test-health-history.jsonl` (`belt/state/` already gitignored), pure analysis functions + hermetic tests, `--run`/no-run/`--json` modes; afterward wire test-health into violations' `FEEDERS` registry (small; ask for spec). Also deferred: PART B in Cowork — add the handed-off violations startup step to the ignite `SKILL.md`, verify via a live ignite run. Optional follow-ups: normalize `/park` to full-timestamp filenames as belt-and-suspenders against the mtime-checkout fragility; investigate/remove the nested `/Open-Case/` repo; set `ANTHROPIC_API_KEY` if touching LLM code.

## What is true right now
Branch `feature/build-delta-belt-tool`; IGNITION session `20260710T042926Z-build-violations-block` active until this park. Two belt tools now committed on this branch: delta (`8f95399`) and violations (`97425ad`) — still unmerged to `main`. violations (`belt/violations.py`) is a thin, deterministic, read-only, fail-loud, stdlib-only aggregator: runs belt feeders (v1: delta only, via subprocess + JSON), maps facts to findings through the PURE `delta_findings()` function, emits a `### Violations` block + `--json`, and is strictly non-gating (findings never change the exit code; non-zero only if the aggregator itself fails). Feeders live in a `FEEDERS` registry for one-line extension; a downed feeder becomes an operational note, not a crash. 11 belt tests pass (4 delta + 7 violations). Real-repo run currently shows `[warn] constraints.md changed` (a KNOWN false alarm — see below), plus `[notice] 13 uncommitted files` and `[notice] 1 commits on feature/build-delta-belt-tool not yet on main`.

## What changed in the last session
Built and committed belt tool #2, violations, with tests. Verified with a before/after cross-check table proving every finding traces to a real delta fact AND every absent finding is correctly absent (canon: False → no finding; current-state changed → deliberately ignored; deps: False → no finding). Rule logic factored into a pure `delta_findings(delta_json)` function so it's unit-testable without git; feeder-down resilience tested via monkeypatch; one integration test drives the real subprocess path against a temp git repo. Diagnosed a design issue the real run exposed: the `constraints.md changed` warn is a PERMANENT false alarm because `.memory/` is untracked, so delta's `commits-since ∪ dirty` sees constraints.md as working-tree-dirty on every run. This makes tracking `.memory/` load-bearing (a permanent warn is a warn you learn to ignore), not mere tidiness.

## What needs to happen next
Combined next session (spec written, saved to file): PART A — merge `feature/build-delta-belt-tool` into `main` (fast-forward expected); gitignore `ignition_data/` (runtime receipts), investigate the nested `Open-Case/` dir before touching it, gitignore `.mcp.json` (machine-specific abs paths), decide track-vs-ignore for `scripts/run_cruz_once.sh`; then `git add .memory/` + commit (NO `.memory/canon.md` exists — canon is in loadmem's store; check `constraints.yaml` for secrets before committing). ACCEPTANCE (careful, non-obvious): the criterion is `git status --porcelain` no longer listing `.memory/constraints.md` — NOT `constraints_changed: False`. The tracking commit is itself a change-since-anchor, so `constraints_changed` legitimately stays True via the commits-since path until the NEXT park writes a new anchor past that commit; render as a before/after table. HARD GATE before Part B. PART B — wire `python belt/violations.py` into the ignite plugin's DURABLE SKILL.md (different codebase, not belt-test-covered) as the FIRST dashboard block above Keysmith; verify via a LIVE ignite run, not pytest. Note: after THIS park writes a new anchor, the transient constraints_changed from A4 clears and future violations runs show `constraints_changed: False` permanently. Then: `test-health` — the next belt tool and first feeder adding a new kind of signal beyond delta.

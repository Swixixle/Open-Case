## What is true right now
Branch `feature/build-delta-belt-tool`, one commit (`8f95399`) ahead of `main`; IGNITION session `20260710T040722Z-build-delta-belt-tool` active until this park. Belt tool #1, `delta`, is built and committed: `belt/delta.py` + `belt/tests/test_delta.py` (491 insertions). delta is a deterministic, read-only, stdlib-only git+fs script answering "what changed since last session?" — emits a human `### Delta` report and `--json`; exits 2 (fail-loud) outside a git repo. Anchor = newest `.memory/sessions/session-*.md`, parsing both the full-timestamp (`session-...T..Z.md`) and date+slug (`session-YYYY-MM-DD-<slug>.md`) filename forms, else a 7-day fallback; the anchor used is always printed. All 4 hermetic tests pass (0.44s). Runs under `uv run python` (repo has no bare `python`; Python 3.14.3, pytest 9.0.2). Only `belt/` was committed; `.memory/`, `ignition_data/`, `.mcp.json`, `Open-Case/`, `scripts/` remain untracked.

## What changed in the last session
Built and committed belt tool #1, `delta`. Started an IGNITION session ("build delta belt tool") which created branch `feature/build-delta-belt-tool`. Wrote `belt/delta.py` — repo root resolved from cwd (not `__file__`) so tests can target a throwaway temp repo; "changed since anchor" is computed as commits-since ∪ working-tree-dirty so uncommitted memory/dependency changes are caught. Wrote `belt/tests/test_delta.py` with 4 hermetic tests: commit/dirty/anchor detection, newest-anchor selection across both filename styles, the no-session 7-day fallback, and fail-loud outside a git repo. Verified end-to-end: tests pass, and a real-repo run produced accurate output cross-checked against raw `git status --porcelain` (12→11 untracked after the commit; delta correctly reported its own commit). Committed only `belt/` as `8f95399`.

## What needs to happen next
Merge `feature/build-delta-belt-tool` into `main` (or open a PR) so delta lands, then start the next belt tool. Deferred from delta's scope by design: PR/code-review reconciliation (needs a GitHub PR-list source the MCP lacks) and wiring delta into the ignite dashboard (still standalone). Housekeeping: the untracked `.memory/` + `ignition_data/` trees show up as an uncommitted gap on every delta run — decide whether to commit or gitignore them.

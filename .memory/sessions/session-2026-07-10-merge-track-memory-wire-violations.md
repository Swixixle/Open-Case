## What is true right now
Branch `main` at commit `a684606`; IGNITION session `20260710T054237Z-merge-belt-tools...` active until this park. Both belt tools (delta `8f95399`, violations `97425ad`) are now merged into `main` via fast-forward. Two further commits on main: `df9d407` (gitignore session runtime/machine-specific paths) and `a684606` (track project memory under `.memory/`). Working tree clean. `.memory/` is now git-tracked (constraints.md, constraints.yaml, current-state.md, glossary.md, roadmap.md, sessions/, plus the stray 07-05 log; canon.md was already tracked). Gitignored: `ignition_data/`, `belt/state/`, `/.mcp.json`, `/scripts/run_cruz_once.sh`, `/Open-Case/`. The permanent `constraints.md changed` false alarm is FIXED — constraints.md is tracked & clean (no longer dirty); the lone residual `constraints_changed: True` is via commits-since (the tracking commit a684606) and clears at THIS park's new anchor. `/park` command fixed to `--parking leave` (v0.8.0 `stop` has no `--note`). 11 belt tests still green.

## What changed in the last session
Phase 0: fixed `~/.claude/commands/park.md` Step 6 → `--parking leave` (v0.8.0 `stop` has no `--note`; `--parking note` hangs on a headless editor). Phase A: fast-forward merged `feature/build-delta-belt-tool` → `main`; added `.gitignore` entries for `ignition_data/`, `belt/state/`, `/.mcp.json`, `/scripts/run_cruz_once.sh`, `/Open-Case/` (commit `df9d407` — caught+fixed a bug where inline trailing `#` comments silently no-op'd 4/5 patterns; git `.gitignore` has no inline comments, re-verified all 5 with `git check-ignore`); tracked `.memory/` (commit `a684606`, no secrets). Verified via before/after table: `git status --porcelain` no longer lists constraints.md (load-bearing PASS); residual `constraints_changed: True` proven attributable to commits-since (a684606), not dirty. Investigation: nested `/Open-Case/` is a FULL separate git repo (~Apr 26–27) — gitignored, not removed; `scripts/run_cruz_once.sh` embeds a LIVE bearer token — gitignored, recommend rotating; the spec's "no `.memory/canon.md`" premise was WRONG (it exists AND was already tracked — that's why canon_changed is False). Phase B (wire violations into ignite dashboard): BLOCKED — no durable ignite SKILL.md exists on disk in Claude Code (ignite runs transiently in Cowork/Desktop; not in this session's skill list; `ignite-inline` plugin dir empty; installed_plugins.json lists only swift-lsp + vercel). Decision: defer Part B to Cowork; exact wiring snippet handed off.

## What needs to happen next
PART B (deferred to Cowork, live-run-verifiable): in a Cowork/Claude Desktop session where the ignite skill lives, add the provided final startup step that runs `uv run python belt/violations.py` and renders its `### Violations` output as the FIRST dashboard block above Keysmith (with a fallback line if not a belt repo); verify via a fresh ignite run on Open-Case showing the block on top. Then SESSION 2: build `test-health` (belt tool #3) — first feeder adding a new signal beyond delta; gitignored append-only history at `belt/state/test-health-history.jsonl` (`belt/state/` already gitignored this session), pure analysis functions + hermetic tests, `--run`/no-run/`--json` modes. After that: wire test-health into violations' FEEDERS registry (small; ask for spec). NB: this park writes a new anchor past a684606, so future violations runs show `constraints_changed: False`.
Housekeeping from investigation: rotate the bearer token in `scripts/run_cruz_once.sh`; investigate/remove the nested `/Open-Case/` repo.
Older still-open: `ANTHROPIC_API_KEY` if touching LLM code; a durable ignite plugin (now more than optional — it's the prerequisite that blocked Part B from Claude Code).
